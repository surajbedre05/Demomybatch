//The filter() method in JavaScript is used to create a new array containing only the elements that pass a given test (condition).

//It does not change the original array — it just returns the filtered one.

let numbers = [1, 2, 3, 4, 5];

let evenNumbers = numbers.filter(num => num % 2 === 0);

console.log(evenNumbers); // [2, 4]

console.log("3"+5+6+"6"+7);

console.log(6+undefined)